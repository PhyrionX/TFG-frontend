version: '3.7'

services:

  sample:
    container_name: sample
    build:
      context: .
      dockerfile: dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 9000:80
    environment:
      - CHOKIDAR_USEPOLLING=true